default_stages: [pre-commit]

repos:
  - repo: local
    hooks:
      - id: fmt
        name: Format and lint
        entry: bash -lc 'make format'
        language: system
        pass_filenames: false
