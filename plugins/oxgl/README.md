# oxgl

GitLab workflow skills using the `glab` CLI.

## Skills

- **`/open-mr`** — create branch, commit, push, open merge request
- **`/issue`** — create issues with type labels, milestone, and linked issues
- **`/shipit`** — create MR with auto-merge enabled
